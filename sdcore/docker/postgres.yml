version: "3.9"
services:
  postgres:
    image: postgres:latest
    container_name: event_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: events
    ports:
      - "5433:5432"